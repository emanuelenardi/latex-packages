% NOTE 'a4paper' defines the paper size;
% NOTE '12pt' sets the size of the main font in the document. If no option is specified, 10pt is assumed.
% NOTE 'titlepage' specifies whether a new page should be started after the document title or not. The article class does not start a new page by default, while report and book do.
% NOTE 'draft' makes LaTeX indicate hyphenation and justification problems with a small square in the right-hand margin of the problem line so they can be located quickly by a human. It also suppresses the inclusion of images and shows only a frame where they would normally occur;
% \documentclass[
% 	,a4paper
% 	,12pt
% 	,titlepage
% 	% ,draft
% ]{article}

\usepackage{set-utilities}

% NOTE The cmap package is intended to make the PDF files generated by pdflatex 'searchable and copyable' in acrobat reader and other compliant PDF viewers.
\usepackage{cmap}
% NOTE % cmap + mathematics (Unicode)
% \usepackage[noTeX]{mmap}

\begin{comment}
\usepackage{ifxetex}
\ifxetex
	% NOTE compilation with XeLaTeX
	\usepackage[no-math]{fontspec}
	\defaultfontfeatures{
		Ligatures=TeX,
		Numbers=OldStyle,
		Scale=MatchLowercase
	}

	% NOTE babel for XeTeX
	\usepackage{polyglossia}
	\setmainlanguage{italian}

	\usepackage{xltxtra}
	\newcommand{\tex}{\TeX}
	\newcommand{\latex}{\LaTeX}
	\newcommand{\xelatex}{\XeLaTeX}

	% TODO specifica font predefiniti con xelatex
	% NOTE questi sono solo esempi
	% NOTE per cercare i font di sistema usare 'fc-list -f "%{family}\n" | sort -u'
	% \setmainfont{Adobe Garamond Pro}
	% \setmainfont{Butler}
\else
	% NOTE compilation with LaTeX

	% NOTE 'fontenc' - is oriented to output, that is, what fonts to use for printing characters;
	\usepackage[T1]{fontenc}
	% NOTE 'inputenc' - allows the user to input accented characters directly from the keyboard.
	\usepackage[utf8]{inputenc}

	% NOTE lingue usate nel documento
	% NB l'ultima dev'essere la lingua principale del documento...
	\usepackage[english, italian]{babel}
	% NB ...oppure deve essere esplcitato
	\usepackage[main=italian, english]{babel}

	% NOTE introdotto dal progetto calliope
	% \usepackage[default, oldstyle, scale=0.95]{opensans}
	\usepackage[scaled=1]{sourcecodepro}

	% IDEA 'microtype' - migliora il riempimento delle righe
	% NOTE activate = {true,nocompatibility} - activate protrusion and expansion
	% NOTE final - enable microtype; use 'draft' to disable
	% NOTE tracking = true, kerning=true, spacing=true - activate these techniques
	% NOTE factor = 1100 - add 10% to the protrusion amount (default is 1000)
	% NOTE stretch = 10, shrink = 10 - reduce stretchability/shrinkability (default is 20/20)
	\usepackage[
		,babel
		% ,activate	= {true,nocompatibility}
		,activate	= {true}
		,final
		% ,draft
		,tracking	= true
		,kerning	= true
		,spacing	= true
		,factor		= 1100
		,stretch	= 10
		,shrink		= 10
	]{microtype}
\fi
\end{comment}

% \usepackage{set-color}
\usepackage{set-date}
\usepackage{set-graphics}

% WARNING package deprecatig
% OPTIMIZE footmisc -> TODO
% OPTIMIZE todonotes -> crea overhead

% TODO implementare in tutti i file
\usepackage{set-geometry}
% \usepackage{set-highlights}
% \usepackage{set-listing}
% \usepackage{set-minted}
\usepackage{set-lists}
% \usepackage{set-notes}
\usepackage{set-math}
\usepackage{set-pages}
\usepackage{set-paragraph}
\usepackage{set-quote}
% \usepackage{set-rotating}
\usepackage{set-tables}

% \usepackage{set-tikz-externalization}
% \usepackage{set-tikz}
% \usepackage{set-tikz-macros}

% \usepackage{set-forest}
% \usepackage{set-automata}
% \usepackage{set-algorithm2e}
% \usepackage{set-toc}
\usepackage{set-links}
% \usepackage{set-external-files}

% \usepackage{my-macros}

% NB da implementare in tutti i file
% \usepackage{set-pdf-props}
